# SUMMARY — сесія 2026-07-21 → 2026-07-24

Підсумок роботи над AI Trader (GMGN meme-coin скринер + SHADOW авто-трейд бот) за цю сесію.
Технічна специфікація повністю живе в `SPEC.md` — цей файл лише хронологічно й людською мовою
пояснює, що і навіщо робилось, для швидкого введення в курс справи.

## Контекст на старті

Проєкт — форк `GMGNAI/skillmarket-demos`, робота в `aitrader/`. Базова філософія (SPEC.md §2):
людина в контурі — машина відбирає кандидатів, людина натискає "купити". Ключ GMGN підключено
(Enable Reading, Enable Trading вимкнено), дашборд працював локально на Windows у SHADOW-режимі.

## 1. SHADOW-only авто-трейд бот — з нуля до робочого стану

Користувач попросив свідомий, обмежений виняток із правила "людина натискає": окремий шлях,
що **тільки в SHADOW** сам відкриває й закриває паперові позиції на $20, щоб накопичувати
статистику "сигнал → результат". LIVE і ручний флоу лишились без змін.

Стратегія виходу пройшла кілька ітерацій за прямим запитом користувача, фінальна версія:
- **+20%** → частковий тейк (продати 30%, стоп переноситься на беззбиток)
- Далі — **без фіксованої стелі**, тільки трейлінг-стоп **25%** від піку ("не закриваємо на
  150%, просто піддягуємо стоп-лос")
- Початковий жорсткий стоп **-35%** (діє лише до часткового тейку)
- Реалізований PnL на стопі **обрізається рівно по порогу** — щоб прослизання між циклами
  сканування (~5.6с) не могло дати гірший результат, ніж заявлений стоп (реальний кейс:
  токен TEXE дав -67.1% замість -35% до цього фіксу)
- Повторний вхід у той самий токен-адресу — **назавжди заблокований** (не лімітований по часу
  кулдаун, а перманентний чорний список `ST.auto_traded_addresses`, відновлюється з логів
  при рестарті)
- Вхід у токени старші 3 днів — заблокований, за винятком дуже сильного сигналу
  (conviction ≥ 0.9 і priority ≥ 80)
- Кількісні ліміти (максимум позицій, загальна експозиція, ліміт авто-позицій) —
  **навмисно прибрані повністю** за прямим запитом користувача, щоб не гальмувати накопичення
  статистики. Якісні фільтри при цьому не послаблено — навпаки, посилено (нижче).

Також прибрано (за явним запитом користувача): денний ліміт збитку та kill-switch на 3 збитки
поспіль — "бот повинен торгувати безперервно, щоб накопичувати статистику".

## 2. GitHub — збереження прогресу як точки відкату

Немає прав пушити напряму в `GMGNAI/skillmarket-demos`, тож створено форк
`Prihapython/skillmarket-demos` (через GitHub API), увесь код регулярно комітиться й пушиться
туди. Локальний `~/.claude/settings.json` отримав `Bash(git push *)` дозвіл, щоб автоматизація
не блокувалась на кожному пуші.

## 3. Скіли з маркетплейсу GMGN

Проаналізовано кілька скілів на `gmgn.ai/ai?tab=skills_market`: "Token Basic Info",
"Get X Following Posts" (потребує X/Twitter dev-акаунт, який виявився заблокованим),
"5-Min Trending Tokens". Висновок сесії: продовжувати розвивати власний код скринера/бота
замість збирання маркетплейс-скілів — наявна логіка гнучкіша й прозоріша для налагодження.

## 4. Критичний баг: подвійний вхід + прослизання стопу

Реальний інцидент (скріншот користувача): токен TEXE дав -67.1% замість заявленого стопу -35%.
Причина — бот перевіряв ціну лише раз на ~5.6с, і фактичний вихід стався за гірше визначеної
межі ціни. Виправлено clamp'ом PnL до порогу стопу (див. вище) + перманентний чорний список
адрес замість лічильника з тайм-аутом (щоб той самий токен не міг зайти повторно з новим
гіршим результатом).

## 5. Деплой на сервер (Hetzner VPS)

Мотивація користувача: "постійно вимикають світло вдома" — потрібна незалежна від локальної
машини платформа.

- **CX23**, Гельсінкі, $6.49/міс, 2 vCPU / 4GB RAM / 40GB
- IP `62.238.58.73`, Ubuntu, юзер `aitrader`
- `systemd`-юніт `aitrader.service`, `nginx` reverse-proxy + HTTP Basic Auth спереду, `ufw`
- SSH-ключ `ed25519`, вирішена проблема з вставкою ключа (розбивався на кілька рядків)
- Git-флоу деплою: `git push fork main` локально → на сервері `git pull && systemctl restart
  aitrader`
- Домовленість: локальний і серверний інстанс **ніколи** не тримати з AUTO увімкненим
  одночасно (дублюючі угоди)

## 6. Довгий, не до кінця діагностований баг: AUTO показував "ВИМК"

Годинами після деплою фронтенд показував AUTO як вимкнений, хоча бекенд підтверджував
`auto_trade:true`. Перевірено й відкинуто кілька гіпотез (застарілий деплой, плутанина вкладок).
Застосовано захисний фікс — фронтенд примусово ресинхронізується щороку опитування — але корінь
проблеми на той момент лишився не до кінця з'ясованим. **Ретроспективно з'ясовано пізніше в цій
же сесії** (див. пункт 15) — найімовірніша справжня причина: опитування могло тихо "вмирати"
після одної невдалої відповіді й ніколи не відновлюватись, заморожуючи UI на застарілому стані.

## 7. `/code-review high --fix` — повний прогін

828-рядковий діф, 8 паралельних агентів-шукачів, 10 підтверджених знахідок, 9 виправлено
(pnl-неузгодженість між позиціями й логом, застаріла severity після помилки запиту → хибні
escape-продажі, подвійний підрахунок часткових угод у вінрейті, "зомбі"-позиції з нульовою
ціною входу, UI-регресія, яку сам ввів раніше в сесії, тощо). Одна знахідка свідомо пропущена
(мертвий, але нешкідливий `RiskManager.consec_losses`-стан) — занадто інвазивно чистити заради
косметики.

## 8. Перше скидання статистики

Після виправлення багів історичні дані змішували стару стратегію й старі баги — вирішено
почати накопичувати вінрейт заново. Відкриті позиції закрито, `trade_decisions.jsonl`
заархівовано (не видалено), лічильники обнулено.

---

## 9. Ця сесія (видима частина): аудит "порахували, але не застосували"

Користувач показав скріншот токена "claude" — авто-бот купив токен, що вже впав **98% від
ATH**. Розслідування показало патерн, який далі системно шукали по всьому пайплайну відбору:
сигнал **рахується і навіть показується користувачу**, але ніде не впливає на рішення купувати.

Знайдено й виправлено:
- **`crowdedness=="crowded"`** (власна мітка LLM-судді "пізній/мертвий рух") — рахувалась,
  показувалась у статистиці, але ніколи не блокувала вхід. Тепер блокує.
- **`sniper_count > 5`** (кількість снайпер-гаманців) — був лише текстовим тегом. Тепер блокує
  авто-вхід.
- **`liquidity < $3000`** — мінімальної ліквідності для авто-входу не було взагалі. Тепер є.
- Побічний ефект фіксу ліквідності: **Mock-режим (демо без ключа) взагалі не мав поля
  liquidity** → власний новий гейт блокував би там усі угоди. Виправлено додаванням
  реалістичної ліквідності в мок-дані.
- **Детекція виведення ліквідності (rug) на вже відкритих позиціях** — раніше це був лише
  коментар-TODO в коді ("流动性撤离应看 liquidity，后续再加"), тепер `assess_escape` реально
  порівнює поточну й вхідну ліквідність і б'є тривогу при падінні вдвічі.
- **Змішування валют у ліміті ризику**: `RiskManager.exposure()` підсумовував розміри позицій
  різних ланцюгів (SOL/BNB/ETH) так, ніби 1 одиниця кожної коштує однаково — хоча це зовсім
  різні гроші. Оскільки торгівля реально йде тільки на SOL, найпростіший і безпечний фікс —
  повністю заборонити відкриття угод на інших ланцюгах (`TRADEABLE_CHAINS=("sol",)`).

## 10. Кількісні ліміти прибрано вдруге і остаточно

За прямим запитом користувача ("прибери обмеження в SOL"): ліміт кількості авто-позицій,
загальної експозиції, кількості одночасних позицій — `RiskManager.gate()` тепер завжди
дозволяє. Явно підтверджено з користувачем: якісні фільтри (усе з пункту 9) залишаються й
надалі посилюються — мета сесії сформульована прямо: **максимізувати вінрейт у SHADOW, щоб
перейти на реальну торгівлю**.

## 11. Статистика по днях/тижнях/місяцях

Додано нову розбивку в `GET /api/stats/auto` (`by_day`/`by_week`/`by_month`, локальні
календарні межі UTC+3) + перемикач-таби в UI під заголовком статистики.

## 12. UI: панелі, що розтягуються

- Спроба через нативний CSS `resize:vertical` — не спрацювала надійно (конфлікт із flex-версткою).
- Переписано на власний JS drag-хендл (мишею й дотиком) під кожним блоком.
- Хендл довелось зробити помітнішим (спершу 10px темна лінія загубилась, коли користувач
  розтягнув блок і не міг знайти, куди тягнути назад) — збільшено, підсвічується лаймовим.
- Блок статистики переставлено **над** таблицею скринінгу в лівій колонці (за прямим запитом —
  користувач тепер більше дивиться на вінрейт, ніж на ручний вибір токенів).

## 13. Кілька повторних "закрити всі угоди + скинути статистику"

За ходом сесії користувач кілька разів просив: закрити всі відкриті позиції, заархівувати лог
угод, обнулити лічильники — щоразу після чергової партії фіксів у гейтах відбору, щоб нова
статистика рахувалась вже на виправленій логіці.

## 14. Знайдено: бот фактично НЕ торгував автономно

Ключове відкриття сесії. Перевірка логів сервера показала: реальні цикли сканування
(`POST /api/run`) **повністю припинялись**, щойно закривалась/переставала бути активною
вкладка браузера — бо весь пайплайн скринінгу й авто-торгівлі запускався **виключно** запитом
із фронтенду. Бекенд на сервері технічно "працював" (systemd показував active), але нічого сам
не робив. Це прямо суперечило меті деплою на сервер ("щоб торгувало навіть коли вимикають
світло").

**Фікс:** доданий фоновий daemon-тред, що стартує безумовно при запуску FastAPI
(`@app.on_event("startup")`) і сам, незалежно від будь-якого браузера, кожні `DEFAULT_POLL_S`
(5.6с) перевіряє `ST.mode=="SHADOW" and ST.auto_trade` і за потреби викликає `screen_once("sol")`
під тим самим `ST.lock`, яким користується і браузерний шлях. Коли AUTO вимкнено — тред просто
спить, не витрачаючи квоту CLI. **Підтверджено наживо**: моніторингом логів сервера побачено
нові FILTER/REJECT записи, що з'являлись самі, без жодного відкритого браузера.

## 15. Знайдено й виправлено: опитування фронтенду могло тихо "вмирати"

При розслідуванні п.14 виявлено суміжний баг: `scanCycle()` при **будь-якій** невдалій
відповіді бекенду (мережевий збій, момент рестарту) назавжди виставляв `bePolling=false` —
і ніщо його назад не вмикало. Зовнішній `setInterval` продовжував "цокати" щоп'ять секунд, але
щоразу одразу виходив (guard на `!bePolling`), тож сторінка мовчки заморожувалась на застарілих
даних, показавши лише зникаючий toast. Це ймовірно **пояснює і давній нез'ясований баг п.6**
(AUTO показував "вимк" годинами) — якщо опитування тихо померло, `syncAutoTrade()` просто
ніколи більше не викликався.

**Фікс:** невдача більше не вбиває опитування — наступний тик пробує знову; toast показується
лише на перший збій підряд (щоб не спамити), і бейдж MCP стає жовтим з написом "RETRY"/"ПОВТОР"
**і лишається таким**, доки запит реально не пройде успішно — на відміну від toast, який зникав
за кілька секунд і нічого не залишав видимим.

## 16. AUTO не переживав рестарт сервісу

Останнє відкриття сесії: `ST.auto_trade` жив тільки в оперативній пам'яті. Кожен
`systemctl restart` (тобто кожен деплой) тихо скидав його в `False` — і в поєднанні з фіксом
п.14 це означало, що після кожного оновлення коду торгівля мовчки зупинялась, поки хтось не
помічав і не вмикав вручну через API.

**Фікс:** `ST.auto_trade` тепер зберігається у файл `outputs/auto_trade_state.json` при кожній
зміні й відновлюється при старті сервера. `ST.mode` як і раніше **не** зберігається (завжди
стартує з безпечного SHADOW) — відновлений `auto_trade=True` спрацьовує лише в парі з ним, ніколи
мовчки не "оживає" в незнайомому стані. Перевірено наживо подвійним рестартом: після другого
рестарту AUTO увімкнувся сам, без жодного ручного виклику.

## 17. Паралельна сесія запушила зміни напряму в той самий форк

Користувач передав раніший `SUMMARY.md` в **інший чат Claude Code**, і той чат самостійно
проаналізував код і запушив коміт `f8f6c18` напряму в GitHub (без мого відома, знайшов його
через `git fetch` + `git log fork/main`). Зміни виявились якісними й узгодженими з логікою
сесії:
- `crowdedness=="early"` тепер вимагає **реального** руху (5m≥3%, 1h≥5%), а не просто плюсового
  знаку — закриває суміжну діру: "мертвий кіт" з мікровідскоком +1-2% раніше міг пройти як early.
- `assess_escape`: honeypot/допемісія-повернулась/ліквідність-впала-вдвічі тепер **кожен окремо**
  досягає порогу негайної втечі (severity 75/70/70), а не тільки в комбінації з іншим сигналом.
- При невдалому запиту моніторингу severity тепер **зберігає останнє відоме значення** (з міткою
  "застаріло"), а не скидається в 0 — обнулення якраз у момент, коли RPC/індекс падають разом
  із реальним рагом, ховало сигнал у найгірший час.
- Доданий **другий частковий тейк на +50%** (ще 30% від початкового розміру) між існуючим +20%
  і трейлінгом.
- Розмір ручної купівлі й "план виходу", що показується людині, тепер рахуються з **тієї самої**
  CFG, що й авто-бот — раніше вели дві окремі, поступово розбіжні системи чисел.

Задеплоєно на сервер (до того коміт лежав тільки на GitHub, сервер про нього не знав).

## 18. SSH/деплой-доступ для `aitrader` користувача

З'ясувалось, чому інший чат **не зміг** сам задеплоїти свій коміт: у користувача `aitrader` на
сервері взагалі не було директорії `.ssh` — ключ (`~/.ssh/id_ed25519`, коментар
`aitrader-hetzner`) був доданий лише для `root`. Виправлено: створено `/home/aitrader/.ssh`,
скопійовано ключ; додатково видано **обмежений** `sudo` (тільки `systemctl
{start,stop,restart,status,is-active} aitrader`, ніякого повного root). Тепер будь-яка сесія може
деплоїти через `ssh aitrader@62.238.58.73` без пароля й без root.

**Супутня пастка**: якщо деплоїти то через `root`, то через `aitrader`, власник файлів у `.git`
змішується і `git pull` під `aitrader` падає з "insufficient permission for adding an object to
repository database .git/objects" — лікується одноразовим `chown -R aitrader:aitrader` (від root).

## 19. Ще один цикл "порахували, але не заблокували" — знайдено через реальні токени

Продовжуючи метод з пункту 9, розібрали кілька токенів, які бот реально купив чи проігнорував:

- **Gorou** — токен віком 9 хвилин, графік практично порожній, а `conviction=0.95`. Виявилось,
  що `vol_1h` і `swaps` **рахувались, але ніде не перевірялись** жодним порогом. Додано
  `min_auto_swaps=50` (buys+sells разом) і `min_auto_volume_usd=$10000`.
- **Vader** — dev цього токена історично випустив 100 монет, 97% ругнуто, і вже **вийшов саме з
  цього токена** (нульовий баланс, статус "closed"). Композитний `dev_score` все одно дав 0.21,
  вище мінімального порогу 0.15 — бо "вихід із конкретного токена" був лише -0.10 штрафом у
  формулі, а не окремим блоком. Додано незалежний жорсткий гейт `dev.exited==True` — блокує
  вхід самостійно, не залежно від сукупного скору.
- **DIP** — куплено о 14:22:13, за 2 хвилини **до** того, як гейт з dev_score≥0.30 (див. нижче)
  задеплоївся о 14:24:10; dev_score=0.25 не пройшов би новий поріг. Вилетів по стопу -35%
  протягом 3 хвилин — живе підтвердження, що фікс ловить саме такі випадки.
- **URC** — привабливий на вигляд пампуючий токен (+2475% 24h), коректно заблокований новим
  `dev.exited`-гейтом (creator_token_balance=0, статус closed) — підтвердження, що фільтр працює
  і на позитивних кейсах, не тільки на очевидному смітті.

## 20. Математика трейлінг-стопу — змінено на запит користувача

Користувач сам порахував приклад ("якщо пік +125%, то стоп підтягується на +100%, вірно?") — і
виявилось, що стара логіка (`стоп = пік_ціна × (1 - 25%)`) рахує інакше, ніж він очікував:
+125% пік → реальний стоп був би +68.75%, не +100%. Розібрали різницю на кількох прикладах,
підтвердили, що користувач хоче **фіксовані 25 відсоткових пунктів** нижче піку (не 25% від
ціни) — на великих зльотах нова версія фіксує суттєво більше прибутку (пік +200% → стоп +175% за
новою логікою, було б лише +125% за старою). Змінено `_auto_decide_exit()`, перевірено на всіх
обговорених числових прикладах.

## 21. Виграшна угода тихо зникала зі статистики

Користувач помітив: "чому пропала плюсова угода?" — вінрейт показував 0W, хоча буквально годину
тому була угода ACTR, закрита в **+61.7%**. Причина: `_load_auto_sells()` читав лише **останні
20 000 рядків** логу, а автономний цикл пише десятки SCREEN/FILTER рядків щоцикл (~5.6с) — лог
виріс до 29 014 рядків менш ніж за годину, і виграшний запис (рядок 5604) опинився за межами
вікна. Виправлено — тепер читає весь файл (той самий компроміс, що вже був прийнятий для
чорного списку адрес).

## 22. "Запас міцності", а не просто "пройшов поріг"

Користувач сам проаналізував три поспіль програшні угоди (DIH/PRU/Freud) і знайшов патерн: не
"який саме показник поганий", а що **кожен** показник був рівно на межі прохідного мінімуму —
`sm_confluence=1` у всіх трьох (мінімум `min_smart_money_confluence`), `dev_score=0.15` у двох з
трьох (мінімум `min_dev_score`). Система не розрізняла "ледь пройшов" і "впевнено пройшов".
Додано окремі, жорсткіші пороги саме для авто-входу: `min_auto_sm_confluence=2`,
`min_auto_dev_score=0.30` — вище за спільні мінімуми пайплайна, не займаючи ручний флоу.

## 23. Реалістичність цілі 80% вінрейту

Пряме питання користувача "як підняти вінрейт до 80%". Чесна відповідь: для мем-коінів це
нереалістично на рівні окремих угод — навіть хороші токени рутинно просідають на 20-35%
усередині дня. Професійні моментум-стратегії зазвичай прибуткові при 40-55% вінрейту саме
завдяки асиметрії (маленькі контрольовані збитки, рідкісні великі виграші) — що й реалізує
uncapped трейлінг-стоп. Запропоновано фокусуватись на expectancy (сумарний PnL, середній
виграш vs середній програш), а не на самому відсотку W/L.

## 24. Покриття платформ: чи варто виходити за межі pump.fun

Перевірено наживо (без фільтра платформи, топ-100 за обсягом на SOL): **97% — Pump.fun**,
ще 1% pump_mayhem (та сама родина), по 1% pool_meteora та bags. LetsBonk/Moonshot у цьому
зрізі не потрапили в топ-100 взагалі. За полем "exchange" (де реально йде торгівля): pump_amm
63%, ще на бондинг-кривій pump.fun 27%, Meteora 9% разом, **Raydium лише 1%**. Висновок:
розширення сканування на інші лаунчпади дало б мінімальний приріст кандидатів — поточний
фокус на pump.fun-екосистемі вже покриває практично весь реальний обсяг ринку.

---

## Поточний стан на кінець сесії

- Сервер: Hetzner `62.238.58.73`, коміт `196287c`, `aitrader.service` активний
- Деплой тепер через `ssh aitrader@62.238.58.73` (не root) + обмежений `sudo` на сам сервіс
- AUTO: увімкнено, персистентно переживає рестарти, автономний фоновий цикл підтверджено працює
  незалежно від браузера
- Торгівля обмежена SOL-ланцюгом; сканування — тільки pump.fun-екосистема (виправдано: ~97-98%
  реального обсягу ринку)
- Кількісні ліміти позицій/експозиції: прибрані
- Якісні гейти входу (усі додані/посилені за сесію): crowdedness, sniper-кількість, ліквідність,
  обсяг+кількість угод за годину, dev вийшов із конкретного токена, консенсус з запасом (≥2),
  dev-скор із запасом (≥0.30), вік токена, rug-детекція на відкритих позиціях
- Вихід: -35% стоп → +20%/+50% часткові тейки → трейлінг 25 відсоткових пунктів від піку
  (фіксовано, не пропорційно), без стелі зверху
- Статистика: щойно вкотре скинута, короткий трек-рекорд, вінрейт поки малозначущий через малу
  вибірку — аналіз програшів на запит користувача, без автоматичного розкладу

## Що НЕ зроблено / залишається

- Telegram-сповіщення про угоди — обговорювалось, не реалізовано
- HTTPS/Let's Encrypt на сервері — зараз чистий HTTP + Basic Auth
- `entry_signal` у логах не містить sniper_count/liquidity/vol_1h/swaps, хоча вони тепер жорсткі
  гейти — ретроспективний аналіз програшів через це неповний, варто розширити логування
- Перехід на LIVE-торгівлю — прямо не заплановано, поточна мета сесії сформульована як
  передумова: спершу максимізувати вінрейт у SHADOW

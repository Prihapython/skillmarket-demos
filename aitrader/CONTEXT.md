# CONTEXT.md — вхідний брифінг для нового чату

Прочитай цей файл першим. Він пояснює **де ми зараз і чому саме так**, щоб не переробляти
вже пройдене й не ламати рішення, ухвалені на основі реальних даних.

Далі за потреби:
- **`NEXT.md`** — що робити далі + мої теорії та чим їх спростувати. **Читати другим.**
- **`WALLET_PLAN.md`** — підключення гаманця, LIVE-механіка, усі знахідки з реальних угод
- `STRATEGY.md` — повний опис фільтрів і логіки входу/виходу
- `TESTING_PLAN.md` — методика емпіричного налаштування порогів
- `SPEC.md` — технічна специфікація (китайською, детальна)
- `SUMMARY.md` — хронологія попередніх сесій

Стан на **2026-07-30**, коміт `44c04ba`.

---

## Що це за проєкт

Скринер мем-коінів на Solana (pump.fun) з торговим ботом. Дві гілки:

1. **Ручний флоу** — людина тисне «купити». Працює і на папері, і на реальних грошах.
2. **Авто-бот** — сам відбирає, сам купує, сам виходить. Працює і на папері,
   і **на реальних грошах** (розблоковано 2026-07-30 на прямий запит користувача).

---

## Три режими

Один перемикач у правому верхньому куті, три кнопки:

| Режим | Хто входить | Чиї гроші | Хто виходить | Яка статистика росте |
|---|---|---|---|---|
| **ЗБІР ДАНИХ** | бот | папір | бот | паперова |
| **РУЧНИЙ** | ти | **реальні** | бот | реальна |
| **АВТО** | бот | **реальні** | бот | реальна |

**Статистика рахується завжди** — просто в різні відра. Паперова й реальна зберігаються
й показуються **окремо**, бо змішана цифра не описує ні те, ні інше: папір систематично
оптимістичніший (стоп там спрацьовує завжди, прослизання й комісій немає).
Картка підписує, що саме показує, і перемикається разом із режимом.

**Одна машина = один режим.** Сервер у АВТО не збирає паперову статистику, і навпаки.
Логічно тримати збір даних на сервері, а реальну торгівлю ганяти локально —
обидва бачать той самий гаманець.

**Асиметрія, закладена свідомо:** авто-**вхід** можна вимикати, авто-**вихід** — ні.
Позиція з реальними грошима завжди має стоп, незалежно від режиму.

Режим **переживає перезапуск** (`outputs/trading_mode.json`). Якщо на старті є відкриті
реальні позиції — режим примусово повертається в LIVE: не наглядати за відкритими
позиціями небезпечніше, ніж не відкривати нових.

---

## Інфраструктура

- **Сервер:** Hetzner VPS `62.238.58.73`, systemd `aitrader.service`, nginx + Basic Auth
- **Деплой:** `ssh aitrader@62.238.58.73` (НЕ root) → `cd /home/aitrader/skillmarket-demos &&
  git pull && sudo systemctl restart aitrader`
- **Git:** пушити у `fork` (`Prihapython/skillmarket-demos`), не в `origin` — прав немає.
  VS Code показує «N commits ahead» — це нормально, не синхронізувати кнопкою.
- **Мітка збірки** в кутку інтерфейсу (`коміт/час`) — якщо не збігається з сервером,
  у браузері стара сторінка, тисни Ctrl+Shift+R.

### Гаманець

```
3y8ZpTz3utbcX8idkA7povPuhpeYShNYDFT8PXxxpnMt   імпортований Phantom
баланс ~0.147 SOL (~$11)
```

Прив'язаний до GMGN API-ключа, торгівля на ключі ввімкнена, IP обмежені
(`62.238.58.73` + домашня, вона динамічна — при зміні торгівля почне відмовляти,
а читання працюватиме).

⚠️ **Приватний ключ гаманця не потрапляє на наш сервер ніколи.** У `.env` лежить
лише Ed25519-ключ підпису API-запитів — це різні речі.

⚠️ **Рента токен-акаунтів.** Кожна купівля морозить ~0.002 SOL ($0.30) на акаунті токена.
Після продажу акаунт лишається порожнім, і гроші в ньому. Повертаються **тільки якщо
закрити акаунти вручну в Phantom**. При розмірі $2 це 15% з угоди — чистка не опція,
а умова прибутковості.

---

## Логіка: як токен стає угодою

Кожні ~19 с бот бере до 100 найактивніших токенів на SOL (тільки pump.fun-родина)
і проганяє через послідовність воріт. Хоч один провал — відсів.

**Етап 1 — безпека.** honeypot · не відкликано допемісію · податок >10% ·
`rug_ratio` >60% · `bundler` >30% · частка dev >10% · топ-10 >40%.

**Етап 2 — консенсус.** `smart_degen + renowned KOL` ≥ 1.

**Етап 3 — рейтинг.** Оцінка 0-99: momentum 5хв (вага 30) · 1год (12) · купівлі/продажі (18) ·
оборотність (12) · консенсус (12) · безпека (10) · dev-репутація (12).
Падіння за годину на 12%+ множить оцінку на 0.4.

**Етап 4 — dev-скор** (0.0-1.0, кеш 10 хв). Виживання попередніх токенів · штраф за «фабрику» ·
бонус за успіхи лише при високому виживанні · штраф за «перешкірку» · штраф, якщо dev вийшов.

**Етап 5 — LLM-суддя** (детермінований код, не справжній LLM). Аномальне падіння → reject ·
частка купівель <42% → reject · crowdedness (`early`/`late`/`crowded`) · conviction 0.3-0.95.

**Етап 6 — ворота саме для авто-входу:**

| Умова відсіву | Значення | Підстава |
|---|---|---|
| Вік | > **30 хв** | дані; піднято з 15 (`dbb0cd9`, 2026-07-27) заради кількості угод |
| Снайпери | > 10 | дані: зона 10+ дала -191% |
| Ліквідність | < $3000 | $2-угода сама рухала б ціну |
| Обсяг / угоди | < $10k або < 50 | замало даних, співвідношення — шум |
| Капіталізація | < 60% від ATH | «відскок трупа» |
| Консенсус | < 1 | базовий мінімум |
| dev-скор | < 0.2 | дані: на 0.15 рагі втричі частіші |
| Dev ще не вийшов | — | тримає → може злити |
| Вже торгували | — | один токен = один вхід назавжди |

Проходить приблизно **1 токен із 30**.

**Розмір позиції $2**, до **5** одночасно = максимум $10 у ринку.

---

## Стратегія виходу

```
стоп             -35%   (до першого тейку)
+20%  →  продати 30%,   стоп переїжджає в беззбиток
+50%  →  продати 30%
залишок  →  трейлінг: max(беззбиток, пік − 25 п.п.), тільки вгору
escape severity ≥70  →  негайний повний вихід
```

Після першого тейку стоп описується **однією формулою**: `max(беззбиток, пік − 25 п.п.)`.
Другий тейк стоп не змінює. Формула визначена в `trailing_stop_price()` — **єдине місце**,
звідки її беруть і локальний цикл, і ордер на GMGN.

### Як це виконується в LIVE

- Сходинка **живе на боці GMGN** (`swap --condition-orders`): спрацьовує за <0.3 с
  проти наших ~19 с циклу. Це важливо: зливи інсайдерів відбуваються за секунди.
- **Беззбитковий стоп і трейлінг** ставить наш цикл окремим ордером за абсолютною ціною
  (`order strategy create --sub-order-type stop_loss --check-price`), бо `drawdown_rate`
  у GMGN — це відсоток від піку, а в нас процентні пункти; це різна математика.
- **Факт спрацювання тейків визначається за залишком токена на блокчейні**, а не за нашим
  прапорцем: ту угоду виконав GMGN, ми в ній не брали участі.
- **Стоп перевіряється щоцикл.** Якщо він помер — керування повертається локальній логіці.

---

## Що перевірено реальними грошима (8 угод, ~$1.65)

```
✅ купівля, умовні ордери, повний вихід, скасування стратегії
✅ часткові тейки +20% / +50% — спрацювали самі
✅ беззбитковий стоп — врятував угоду з -48% до +8%
✅ прослизання: 0.62% / 1.28% / 1.93%  (поріг 5%)
✅ стоп-лос: спрацював 2 рази з 4, виконав за номіналом (-32.4%, -36.7%)
❌ escape-вихід (honeypot, обвал ліквідності) — жодного разу
```

**Стоп провалюється приблизно раз із трьох** — і не через погане виконання, а тому що
ордер не спрацьовує взагалі (один раз не створився, один раз `failed` при спрацюванні).
Захист від цього стоїть, але причина невідома.

---

## Статистика

| | Угод | Вінрейт | PnL |
|---|---|---|---|
| Паперова (сервер) | 50 | 66% | +$90.92 |
| Реальна (локально) | 8 | 25% | ≈ -$1.35 |

⚠️ **Паперова завищена систематично.** Перерахунок 139 угод із реальними тертями
(стоп -36.9%, прослизання 1.3%, $0.06 витрат) на позиції $20 дає **≈ +$135 замість +$195**.

**Розмір вирішує все** через фіксовані витрати:
```
$2   +$5.61     ← тільки якщо чистити ренту; без чистки -$41.65
$20  +$131.19
```

---

## Головні принципи роботи

**1. Пороги — з даних, не з інтуїції.** Методика в `TESTING_PLAN.md`.
Не міняй пороги «бо логічно» — спершу перевір на даних.

**2. Перевіряй на свіжих даних.** Підібрати фільтр, дивлячись на вже зібрані угоди,
і на них же виміряти покращення — це підгонка. Так «покращується» будь-яка вибірка.
Змінив → накопичуй → дивись на угоди, яких ще ніхто не бачив.

**3. Моки не ловлять інтеграційні баги.** За одну сесію знайдено **18 багів**,
жодного не показали моки. Три втрачали б гроші **мовчки**, з нормальним виглядом
інтерфейсу. Нову ланку GMGN-API вважати робочою лише після реального виклику.

**4. Не роби висновків з 1-3 спостережень.** За день зроблено **п'ять** хибних висновків,
усі в песимістичний бік — брався найгірший випадок і оголошувався типовим. Деталі в `NEXT.md`.

---

## Пастки, на які вже наступали

- **Вінрейт по останньому запису.** Поетапний вихід пише кілька SELL; рахувати треба
  всю угоду разом. Виправлено, вінрейт стрибнув 49.6% → 56.7%.
- **Обрізання PnL по номіналу стопа.** SHADOW моделює ідеальний стоп і пише рівно -35%.
  У LIVE це вимкнено — там пишеться факт.
- **Скидання статистики ≠ видалення журналу.** Кнопка «↺ Скинути» лише пересуває точку
  відліку. **Не використовуй старий флоу «архів + truncate»** — знищиш дані для аналізу.
- **Журнал росте швидко** (252 МБ, 1.67 млн рядків). Статистика читає лише новий хвіст;
  перший виклик після рестарту ~40 с, далі миттєво.

---

## Що на черзі

Повний список і обґрунтування — у **`NEXT.md`**. Стисло:

1. **Тиждень нічого не міняти** — дати вибірці вирости для чесної перевірки гіпотез.
2. Перевірити `rug_ratio`/`top10`/`bundler`/`dev_hold` — зараз є лише в 19 зі 139 угод.
3. Перевірити три гіпотези з `NEXT.md`: вік >20хв, купівлі >70%, снайпери 9-10.
4. З'ясувати, за яких умов GMGN відхиляє `loss_stop`.
5. Не реалізовано: Telegram-сповіщення, HTTPS на сервері (потрібен домен).

⚠️ **Не вмикати авто-LIVE надовго, поки escape-вихід не перевірено на практиці.**
